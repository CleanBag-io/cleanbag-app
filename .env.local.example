# Supabase Configuration
# Get these from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=your-supabase-publishable-key

# Supabase Service Role Key (for webhooks and server-side operations that bypass RLS)
# Get from Supabase > Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Site URL (for email redirect links and Stripe return URLs)
# In development: http://localhost:3000
# In production: https://cleanbag.io
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Stripe Configuration
# Get from Stripe Dashboard > Developers > API keys
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Stripe Webhook Secret
# Get from Stripe Dashboard > Developers > Webhooks, or use Stripe CLI:
#   stripe listen --forward-to localhost:3000/api/webhooks/stripe
STRIPE_WEBHOOK_SECRET=whsec_...

# Google Maps Configuration
# Get from Google Cloud Console > APIs & Services > Credentials
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key
GOOGLE_MAPS_SERVER_API_KEY=your-google-maps-server-key

# Web Push (VAPID) Configuration
# Generate with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
